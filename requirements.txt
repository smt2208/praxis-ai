# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
asyncpg>=0.29.0

# Config
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.0.0

# LangChain core + integrations
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-qdrant>=0.2.0
langchain-tavily>=0.1.0
langchain-text-splitters>=0.3.0

# LangGraph
langgraph>=0.3.0
langgraph-prebuilt>=0.1.0

# Vector DB
qdrant-client>=1.11.0

# Sparse embeddings (for hybrid search)
fastembed>=0.3.0

# LlamaCloud document parsing
llama-parse>=0.5.0
llama-cloud>=0.1.0

# Fast local document extraction (PDF and DOCX — zero API latency path)
pymupdf>=1.24.0        # C-level PDF text extraction; milliseconds vs 5-30s LlamaParse cloud call
python-docx>=1.1.0     # Pure-Python DOCX extraction fallback

# Academic search
arxiv>=2.1.0

# HTTP client (for downloading files in ingestion)
httpx>=0.27.0

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# Rate limiting
slowapi>=0.1.9
